pkgname=declarative.bash
pkgver=0.0.2
pkgrel=1
pkgdesc="A simple library for declarative shell scripts"
license=( BSD )
arch=( any )

package() {
    mkdir -p "$pkgdir/usr/bin"
    cp "$startdir/declarative.bash" "$pkgdir/usr/bin"
}
build() {
  :
}
